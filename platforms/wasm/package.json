{
  "name": "barelymusician",
  "version": "0.7.2",
  "private": true,
  "scripts": {
    "build:bundle": "esbuild demo/main.js --minify --bundle --outfile=build/bundle.js --format=esm",
    "build:processor": "esbuild src/processor.js --minify --bundle --outfile=build/processor.js --format=esm",
    "build": "npm run build:bundle && npm run build:processor && node demo/postbuild.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
